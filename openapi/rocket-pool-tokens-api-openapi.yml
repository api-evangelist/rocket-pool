openapi: 3.2.0
info:
  title: Rocket Pool Protocol API (rocketpool-go) Tokens API
  description: Read-oriented protocol surface exposed by the rocketpool-go Golang library on top of the Rocket Pool smart contracts deployed on Ethereum.
  version: 1.0.0
  license:
    name: GPL-3.0
    url: https://github.com/rocket-pool/rocketpool-go/blob/master/LICENSE
  contact:
    name: Rocket Pool
    url: https://github.com/rocket-pool/rocketpool-go
servers:
- url: https://protocol.rocketpool.example/v1
  description: Logical base path — actual access is via Ethereum smart contracts
tags:
- name: Tokens
paths:
  /tokens/reth:
    get:
      tags:
      - Tokens
      summary: Get rETH Token State
      operationId: getRethState
      responses:
        '200':
          description: rETH token supply and exchange rate
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenState'
  /tokens/rpl:
    get:
      tags:
      - Tokens
      summary: Get RPL Token State
      operationId: getRplState
      responses:
        '200':
          description: RPL token supply and inflation state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenState'
components:
  schemas:
    TokenState:
      type: object
      properties:
        contractAddress:
          $ref: '#/components/schemas/Address'
        symbol:
          type: string
        decimals:
          type: integer
        totalSupply:
          $ref: '#/components/schemas/EthAmount'
        exchangeRate:
          type: number
          format: double
          description: Only populated for rETH
    EthAmount:
      type: string
      description: Amount in wei
    Address:
      type: string
      pattern: ^0x[a-fA-F0-9]{40}$
